import React from "react";
import UploadForm from "../../components/UploadForm"; // Correct the import path

const Home = () => {
  return (
    <div>
      <h1>AI Resume Analyzer</h1>
      <UploadForm />
    </div>
  );
};

export default Home;